   "Allows us to delete previous insertions. IDK why this isn't default tbh
   set backspace=indent,eol,start
  
   "Sets line numbers, how the fuck is this not default.
   set number
   "***END***

  "***TEXT EDITOR SETTINGS***
  set textwidth=79    " Wraps line at 80 characters
  set tabstop=2       " Set tab to 2
 set autoindent
